import 'package:flutter/material.dart';

class CreateLeave extends StatelessWidget {
  const CreateLeave({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(width: 350, height: 400, color: Color(0xFFFCFCFC));
  }
}
